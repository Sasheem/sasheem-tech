import React from 'react';
import styled from 'styled-components';

const About = () => {
    return (
        <div
            style={{
                margin: 'auto 0',
                textAlign: 'center',
                background: 'green',
                height: '100vh',
            }}
        >
            <h1>About Section</h1>
        </div>
    );
};

export default About;